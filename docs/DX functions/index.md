# Data Excellence SQL functions

# Intro

In dit document zullen we een aantal niet standaard SQL functionaliteiten behandelen die in de praktijk toegepast worden. 

Functionaliteiten die behandeld zullen worden zijn:

- ```APPLY```  
- ```PARTITION BY```  
- ```RANK```  
- ```DENSE_RANK``` 
- ```EXISTS```